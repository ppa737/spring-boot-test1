eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
#          uri: https://git.oschina.net/chrywhy/test
          uri: https://github.com/ppa737/spring-boot-test.git
          searchPaths: spring-cloud/helloworldConfig
 #         label: master

  application:
    name: config-server